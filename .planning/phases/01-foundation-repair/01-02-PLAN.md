---
phase: 01-foundation-repair
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - go.mod
  - go.sum
autonomous: true
requirements:
  - FOUND-03
  - FOUND-04
  - FOUND-06

must_haves:
  truths:
    - "go build ./... succeeds with zero errors"
    - "go.mod declares Go 1.24 or later"
    - "Ginkgo is v2.27+ and Gomega is v1.38+"
    - "No Gauge framework dependencies remain in go.mod or go.sum"
    - "go mod tidy completes without errors"
  artifacts:
    - path: "go.mod"
      provides: "Updated Go version, Ginkgo/Gomega versions, no Gauge deps"
      contains: "go 1.24"
    - path: "go.sum"
      provides: "Clean dependency checksum file without Gauge entries"
  key_links:
    - from: "go.mod"
      to: "pkg/oc/oc.go"
      via: "module path resolution"
      pattern: "module github.com/openshift-pipelines/release-tests-ginkgo"
    - from: "go.mod"
      to: "github.com/onsi/ginkgo/v2"
      via: "require directive"
      pattern: "ginkgo/v2 v2\\.(2[7-9]|[3-9])"
---

<objective>
Upgrade the Go toolchain version, upgrade Ginkgo/Gomega to current releases, remove the Gauge repo dependency and all its transitive deps, and verify the project builds cleanly.

Purpose: The current Go 1.23 is out of support, Ginkgo v2.13.0 is 15+ releases behind (missing SpecTimeout, improved JUnit, better parallel support), and the Gauge dependency must be removed now that pkg/oc/oc.go no longer imports from it.

Output: go.mod and go.sum reflect current toolchain versions with a clean dependency tree, and `go build ./...` succeeds.
</objective>

<execution_context>
@/Users/vibhavbobade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vibhavbobade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-repair/01-RESEARCH.md
@.planning/phases/01-foundation-repair/01-01-SUMMARY.md

<interfaces>
<!-- Key state after Plan 01 completes -->
<!-- go.mod module path is: github.com/openshift-pipelines/release-tests-ginkgo -->
<!-- All Go imports use the new module path -->
<!-- pkg/oc/oc.go no longer imports from github.com/openshift-pipelines/release-tests -->
<!-- The direct dependency on release-tests is now unused -->

Current go.mod version lines (will be updated):
```
go 1.23.4
toolchain go1.23.6
```

Current Ginkgo/Gomega versions (will be upgraded):
```
github.com/onsi/ginkgo/v2 v2.13.0
github.com/onsi/gomega v1.29.0
```

Dependencies to REMOVE (no longer imported after Plan 01):
```
github.com/openshift-pipelines/release-tests v0.0.0-20250509122825-94ba39df192b
```

Transitive deps that go mod tidy will remove:
```
github.com/getgauge-contrib/gauge-go v0.4.0
github.com/getgauge/common v0.0.0-20211105093448-e0aee1ccb217
github.com/dmotylev/goproperties v0.0.0-20140630191356-7cbffbaada47
```

CRITICAL: Do NOT modify k8s.io replace directives. They must remain pinned to v0.29.6 for tektoncd/pipeline v0.68.0 compatibility.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade Go version and Ginkgo/Gomega dependencies</name>
  <files>go.mod, go.sum</files>
  <action>
Run the following commands in sequence:

1. **Update Go version in go.mod:**
   ```bash
   go mod edit -go=1.24
   ```
   The `toolchain` directive will be updated automatically by `go mod tidy` to match the system's Go version.

2. **Upgrade Ginkgo and Gomega to latest:**
   ```bash
   go get github.com/onsi/ginkgo/v2@latest
   go get github.com/onsi/gomega@latest
   ```

3. **Remove the Gauge repo dependency** (now unused after Plan 01 fixed oc.go imports):
   ```bash
   go mod edit -droprequire github.com/openshift-pipelines/release-tests
   ```

4. **Tidy to remove transitive Gauge deps:**
   ```bash
   go mod tidy
   ```

5. **Verify no Gauge deps remain:**
   ```bash
   go list -m all | grep -i gauge        # must return nothing
   go list -m all | grep "release-tests$" # must return nothing (only release-tests-ginkgo)
   ```

6. **Verify k8s.io replace directives are intact** (they must still point to v0.29.6):
   ```bash
   grep "k8s.io" go.mod | grep "=>"      # all should show v0.29.6
   ```

CRITICAL: If `go mod tidy` modifies the k8s.io replace directives, restore them to v0.29.6. These are pinned for tektoncd/pipeline v0.68.0 compatibility and must not change.

Do NOT upgrade Tekton, OpenShift, or k8s.io dependencies in this task. Only Go, Ginkgo, and Gomega.
  </action>
  <verify>
    <automated>cd /Users/vibhavbobade/go/src/github.com/openshift-pipelines/release-tests-ginkgo && grep "^go " go.mod | grep -E "1\.(2[4-9]|[3-9][0-9])" && go list -m github.com/onsi/ginkgo/v2 | grep -E "v2\.(2[7-9]|[3-9][0-9])" && go list -m github.com/onsi/gomega | grep -E "v1\.(3[8-9]|[4-9][0-9])" && go list -m all 2>/dev/null | grep -c "gauge" | grep "^0$"</automated>
  </verify>
  <done>go.mod declares Go 1.24+, Ginkgo is v2.27+, Gomega is v1.38+, no Gauge dependencies remain in the module graph</done>
</task>

<task type="auto">
  <name>Task 2: Verify full build and install Ginkgo CLI</name>
  <files>go.mod, go.sum</files>
  <action>
1. **Build the entire project:**
   ```bash
   go build ./...
   ```
   This must succeed with zero errors. If it fails, diagnose and fix the issue -- the most likely cause is a dependency version incompatibility introduced by the Go version bump.

2. **Verify no old import paths remain in compiled code:**
   ```bash
   grep -rn "release-tests/pkg" --include="*.go" .   # must return 0 results (excluding .planning/)
   grep -rn "srivickynesh" --include="*.go" .         # must return 0 results (excluding .planning/)
   ```

3. **Install the Ginkgo CLI** matching the library version:
   ```bash
   go install github.com/onsi/ginkgo/v2/ginkgo@latest
   ```
   Verify with `ginkgo version`.

4. **Verify go.sum has no Gauge entries:**
   ```bash
   grep -c "gauge" go.sum     # should be 0
   ```

If `go build ./...` fails due to breaking changes in newer Ginkgo/Gomega versions, pin to the last compatible versions:
- Ginkgo: `go get github.com/onsi/ginkgo/v2@v2.27.5` (last Go 1.23 compatible, though we're on 1.24+ now)
- Gomega: `go get github.com/onsi/gomega@v1.38.3`
Then re-run `go mod tidy` and `go build ./...`.
  </action>
  <verify>
    <automated>cd /Users/vibhavbobade/go/src/github.com/openshift-pipelines/release-tests-ginkgo && go build ./... && go vet ./...</automated>
  </verify>
  <done>go build ./... succeeds with zero errors, go vet ./... passes, Ginkgo CLI is installed and matches library version</done>
</task>

</tasks>

<verification>
1. `go build ./...` exits 0 (clean build)
2. `go vet ./...` exits 0 (no vet errors)
3. `go list -m github.com/onsi/ginkgo/v2` reports v2.27+ 
4. `go list -m github.com/onsi/gomega` reports v1.38+
5. `grep "^go " go.mod` shows 1.24 or later
6. `go list -m all | grep -i gauge` returns nothing
7. `go list -m all | grep "release-tests "` returns nothing (note trailing space to avoid matching release-tests-ginkgo)
8. `ginkgo version` outputs a version matching the library
9. k8s.io replace directives in go.mod still point to v0.29.6
</verification>

<success_criteria>
- go build ./... succeeds with zero errors on the entire project
- Go 1.24+ declared in go.mod
- Ginkgo v2.27+ and Gomega v1.38+ in go.mod
- Zero Gauge framework dependencies (gauge-go, gauge-common, goproperties) in go.mod or go.sum
- The release-tests direct dependency is removed from go.mod
- k8s.io replace directives unchanged at v0.29.6
- Ginkgo CLI installed and functional
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-repair/01-02-SUMMARY.md`
</output>
