---
phase: 02-suite-scaffolding
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - tests/operator/operator_patterns_test.go
autonomous: true
requirements:
  - SUITE-05
  - SUITE-06
  - SUITE-07

must_haves:
  truths:
    - "A sample spec demonstrates DeferCleanup executing resource teardown co-located with creation, using the correct cleanup-with-context form"
    - "A sample spec demonstrates Eventually polling a condition with timeout and polling interval from pkg/config constants"
    - "A sample spec demonstrates Consistently asserting a condition does not change over a duration"
    - "A sample spec demonstrates an Ordered container with BeforeAll, shared closure variables, and sequential It blocks"
    - "All sample specs compile cleanly as part of the operator test package"
  artifacts:
    - path: "tests/operator/operator_patterns_test.go"
      provides: "Reference implementations of DeferCleanup, Eventually, Consistently, and Ordered patterns"
      min_lines: 60
  key_links:
    - from: "tests/operator/operator_patterns_test.go"
      to: "tests/operator/suite_test.go"
      via: "Same package (operator_test) shares sharedClients var"
      pattern: "package operator_test"
    - from: "tests/operator/operator_patterns_test.go"
      to: "pkg/config"
      via: "Uses config.APITimeout, config.APIRetry, config.ConsistentlyDuration"
      pattern: "config\\.APITimeout|config\\.APIRetry|config\\.ConsistentlyDuration"
---

<objective>
Create reference implementations of DeferCleanup, Eventually/Consistently, and Ordered container patterns in the operator test suite.

Purpose: Future migration phases need working examples of these patterns to copy. These sample specs in tests/operator/ demonstrate the correct Ginkgo v2 patterns for resource cleanup, async polling, and sequential multi-step workflows. Placed in operator suite because operator tests will use all three patterns heavily (cluster-wide state modifications, polling for TektonConfig changes, sequential configuration steps).

Output: One test file with sample specs covering SUITE-05, SUITE-06, and SUITE-07 requirements.
</objective>

<execution_context>
@/Users/vibhavbobade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vibhavbobade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-suite-scaffolding/02-RESEARCH.md
@.planning/phases/02-suite-scaffolding/02-01-SUMMARY.md

<interfaces>
<!-- Key types from Plan 01's suite_test.go that this plan uses -->

From tests/operator/suite_test.go (created by Plan 01):
```go
package operator_test

var sharedClients *clients.Clients  // Initialized in BeforeSuite
```

From pkg/config/config.go:
```go
const (
    APIRetry            = time.Second * 5
    APITimeout          = time.Minute * 10
    ConsistentlyDuration = 30 * time.Second
    TargetNamespace     = "openshift-pipelines"
)
```

From pkg/oc/oc.go:
```go
func CreateNewProject(ns string)
func DeleteProjectIgnoreErors(ns string)
func Create(path_dir, namespace string)
func DeleteResourceInNamespace(resourceType, name, namespace string)
```

From pkg/clients/clients.go:
```go
func (c *Clients) TektonConfig() operatorv1alpha1.TektonConfigInterface
// sharedClients.Operator provides operator API access
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DeferCleanup, Eventually/Consistently, and Ordered pattern specs</name>
  <files>tests/operator/operator_patterns_test.go</files>
  <action>
Create `tests/operator/operator_patterns_test.go` with sample specs demonstrating three patterns. These are REFERENCE IMPLEMENTATIONS that future migration phases will copy. They must compile but do NOT need a live cluster to be useful as references -- use Pending (`P`) prefix or Skip to avoid requiring cluster access during scaffolding.

Package: `operator_test` (same as suite_test.go, shares `sharedClients`).

**Pattern A: DeferCleanup (SUITE-05)**

Create a `Describe("DeferCleanup Pattern", ...)` block with:
- A spec that shows creating a namespace with `oc.CreateNewProject()` and immediately registering cleanup with `DeferCleanup(func() { oc.DeleteProjectIgnoreErors(ns) })`
- A comment explaining LIFO ordering and that cleanup runs even on failure
- A comment showing the WRONG pattern (capturing SpecContext) vs the RIGHT pattern (callback-with-context form)
- Use `PDescribe` or `Skip("pattern demonstration -- requires cluster")` so it does not fail without a cluster

**Pattern B: Eventually/Consistently (SUITE-06)**

Create a `Describe("Eventually/Consistently Pattern", ...)` block with:
- A spec showing `Eventually(func(g Gomega) { ... }).WithTimeout(config.APITimeout).WithPolling(config.APIRetry).Should(Succeed())` -- use a realistic example like polling TektonConfig status
- A spec showing `Consistently(func() <type> { ... }).WithTimeout(config.ConsistentlyDuration).WithPolling(config.APIRetry).Should(Equal(<value>))` -- show asserting a condition does not change
- Both must reference `config.APITimeout`, `config.APIRetry`, and `config.ConsistentlyDuration`
- Use `PDescribe` or `Skip` to avoid cluster requirement

**Pattern C: Ordered Container (SUITE-07)**

Create a `Describe("Ordered Container Pattern", Ordered, ...)` block with:
- `var` declarations for shared state in the Describe closure scope (NOT initialized there)
- `BeforeAll` that initializes the shared state (e.g., creates a namespace, stores a reference)
- `DeferCleanup` inside `BeforeAll` for cleanup
- Two or more `It` blocks that depend on each other sequentially
- A comment explaining that if any `It` fails, subsequent `It` blocks are skipped
- Use `PDescribe` or `Skip` to avoid cluster requirement

Import only what is needed: ginkgo/v2, gomega, pkg/config, pkg/oc (for function signatures). Use `_ = sharedClients` or reference it in a comment to show it is available from suite_test.go.

Do NOT import `fmt` or `uuid` unless actually used -- avoid `go vet` import errors.
  </action>
  <verify>
    <automated>cd /Users/vibhavbobade/go/src/github.com/openshift-pipelines/release-tests-ginkgo && go vet ./tests/operator/...</automated>
  </verify>
  <done>tests/operator/operator_patterns_test.go exists with DeferCleanup, Eventually/Consistently, and Ordered pattern demonstrations. All three patterns use correct Ginkgo v2 API. File compiles and passes go vet.</done>
</task>

</tasks>

<verification>
1. `go vet ./tests/operator/...` exits 0
2. `operator_patterns_test.go` contains `DeferCleanup` usage with correct pattern
3. `operator_patterns_test.go` contains `Eventually` with `config.APITimeout` and `config.APIRetry`
4. `operator_patterns_test.go` contains `Consistently` with `config.ConsistentlyDuration`
5. `operator_patterns_test.go` contains `Ordered` decorator with `BeforeAll` and multiple `It` blocks
6. No imports are unused (go vet clean)
</verification>

<success_criteria>
- DeferCleanup pattern is demonstrated with correct cleanup-with-context form and LIFO explanation
- Eventually pattern uses config.APITimeout and config.APIRetry for timeout and polling
- Consistently pattern uses config.ConsistentlyDuration
- Ordered container has BeforeAll, shared closure vars, and sequential It blocks
- File compiles as part of the operator_test package
</success_criteria>

<output>
After completion, create `.planning/phases/02-suite-scaffolding/02-02-SUMMARY.md`
</output>
