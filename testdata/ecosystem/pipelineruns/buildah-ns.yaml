apiVersion: tekton.dev/v1
kind: PipelineRun
metadata:
  name: buildah-ns-run
spec:
  pipelineRef:
    name: buildah-ns-pipeline
  timeouts: 
    pipeline: 8m
  workspaces:
  - name: source
    persistentVolumeClaim:
      claimName: shared-pvc